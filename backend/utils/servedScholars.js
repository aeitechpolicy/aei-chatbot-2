// Scholars this bot serves — each has a knowledge_base/ domain.
// Shared by the slim-index build script and aeiScraper.js's runtime loader
// so the two can't drift out of sync.
module.exports = ['Brent Orrell', 'Clay Calvert', 'Shane Tews', 'Will Rinehart'];
